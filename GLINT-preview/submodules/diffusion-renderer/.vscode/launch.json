{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Run Inverse Renderer Inference",
        "type": "debugpy",
        "request": "launch",
        "program": "${workspaceFolder}/inference_svd_rgbx.py",
        "console": "integratedTerminal",
        "env": {
          "CUDA_HOME": "${env:CONDA_PREFIX}",
          "PYTHONPATH": "${workspaceFolder}"
        },
        "args": [
        ]
      }
    ]
  }

